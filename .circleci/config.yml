version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:9

    steps:
      - checkout
      - run: cd backend
      - run: npm test
# orbs:
#   node: circleci/node@3.0.0
# workflows:
#   node-tests:
#     jobs:
#       - node/test
