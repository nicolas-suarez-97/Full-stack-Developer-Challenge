version: '3'
services:
  db:
    image: 'postgres:latest'
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: 'postgresDB'
      POSTGRES_USER: 'app_user'
      POSTGRES_PASSWORD: 'app_password'
